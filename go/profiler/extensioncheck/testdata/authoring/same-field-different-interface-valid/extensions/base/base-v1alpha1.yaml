apiVersion: runtimeconditions.io/v1alpha1
kind: RuntimeConditionsExtensionDefinition

metadata:
  uri: https://example.com/runtimeconditions/scoped-field-base
  version: v1alpha1

spec:
  kinds:
    - name: scoped.service
  interfaceTypes:
    - name: scoped.http
      targetKind: scoped.service
    - name: scoped.queue
      targetKind: scoped.service
