apiVersion: runtimeconditions.io/v1alpha1
kind: RuntimeConditionsExtensionDefinition

metadata:
  uri: https://example.com/runtimeconditions/option-outside-scope
  version: v1alpha1

spec:
  kinds:
    - name: scoped.cache
    - name: scoped.queue
  conditionFields:
    - name: configuration
      appliesToKinds:
        - scoped.cache
  fieldValues:
    - field: configuration.env[].property
      targetKind: scoped.cache
      values:
        - url
