apiVersion: runtimeconditions.io/v1alpha1
kind: RuntimeConditionsExtensionDefinition

metadata:
  uri: https://example.com/runtimeconditions/filesystem-overlap-app
  version: v1alpha1

spec:
  dependencies:
    - https://example.com/runtimeconditions/filesystem-overlap-posix:v1alpha1
  schemas:
    - id: filesystem-overlap-policy
      description: Filesystem overlap policy.
      schema:
        type: object
