version: "3.9"

services:
  http-service:
    build:
      context: .
      dockerfile: ./apps/server/Dockerfile
    labels:
      - "app.role=web-server"
      - "app.tier=backend"
      - "app.component=api"
    environment:
      - ADI_PROFILE=local
      - ADI_PROFILE_NAME=http-service
      - HTTP_PORT=8080
      - NATS_URL=nats://nats-server:4222
    ports:
      - "8080:8080"
    depends_on:
      nats-server:
        condition: service_healthy

  postgres:
    image: postgres:15-alpine
    labels:
      - "app.role=database"
      - "app.tier=data"
      - "app.component=relational-db"
    environment:
      - ADI_PROFILE=local
      - ADI_PROFILE_NAME=postgres
      - POSTGRES_USER=testuser
      - POSTGRES_PASSWORD=testpass
      - POSTGRES_DB=testdb
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U testuser -d testdb"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    labels:
      - "app.role=cache"
      - "app.tier=data"
      - "app.component=key-value-store"
    environment:
      - ADI_PROFILE=local
      - ADI_PROFILE_NAME=redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  nats-server:
    image: nats:2-alpine
    labels:
      - "app.role=message-broker"
      - "app.tier=infrastructure"
      - "app.component=message-bus"
    environment:
      - ADI_PROFILE=local
      - ADI_PROFILE_NAME=nats-server
    ports:
      - "4222:4222"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8222/healthz"]
      interval: 5s
      timeout: 5s
      retries: 5
    command: ["--http_port", "8222"]

  request-logger:
    build:
      context: .
      dockerfile: ./apps/request-logger/Dockerfile
    labels:
      - "app.role=logger"
      - "app.tier=backend"
      - "app.component=event-consumer"
    environment:
      - ADI_PROFILE=local
      - ADI_PROFILE_NAME=request-logger
      - NATS_URL=nats://nats-server:4222
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      nats-server:
        condition: service_healthy
      redis:
        condition: service_healthy

  traffic:
    build:
      context: .
      dockerfile: ./apps/traffic/Dockerfile
    labels:
      - "app.role=test-client"
      - "app.tier=testing"
      - "app.component=load-generator"
    environment:
      - ADI_PROFILE=local
      - ADI_PROFILE_NAME=traffic-generator
      - TARGET_HOST=http-service
      - HTTP_PORT=8080
      - TOTAL_REQUESTS=10
      - REQUEST_DELAY_MS=500
      # Database connection settings
      - POSTGRES_HOST=postgres
      - POSTGRES_PORT=5432
      - POSTGRES_USER=testuser
      - POSTGRES_PASSWORD=testpass
      - POSTGRES_DB=testdb
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      http-service:
        condition: service_started
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      request-logger:
        condition: service_started
